# 漢字書き順カード & ドリル解答PDF生成 — 要件仕様書

Claude Code への依頼文書。このリポジトリの設計・実装はこの仕様を満たす範囲で自由に判断してよい。

## 1. 背景と目的

小学校低学年の子に漢字の書き順を教えるための教材リポジトリを作る。用途は2つ。

1. **カード資産**: 小学1〜3年の配当漢字 440字(1年80 / 2年160 / 3年200)を、1字1カードで生成しリポジトリで管理する。
2. **日常運用**: 塾などで漢字ドリルの課題が出たとき、親が問題の写真を Claude Code に渡す。Claude Code が問題を読み取り、**解答として書くことになる漢字**を特定し、その漢字のカードだけを束ねた**B4印刷用の1つのPDF**を生成する。

## 2. カード仕様

見本(同梱 `kakijun_sample.html`、動作確認済みの生成ロジックは `gen_sample.py`)を踏襲する。

- **一画ずつ分解したコマ**を並べる: 先頭に完成形、続いて「1画目」「2画目」…。各コマでは、書き終えた画をグレー、いま書く画を赤(やや太く)、書き始め位置に赤丸+白抜き番号。マスには十字リーダー(点線)を入れる。
- **読み仮名を載せる**: 音読み=カタカナ、訓読み=ひらがな(送り仮名は「おく-る」のように区切るなど、小学生が読める表記)。ドリルは「よみがな→漢字」の書き取り問題が多いため、読みからカードを探せることが重要。小学生向けの主要な読みに絞ってよい(常用外の読みは不要)。
- **メタデータ**: 各字について 文字・学年・画数・読み一覧 を機械可読な形(JSON等)で持つ。写真から特定した漢字→カードの検索、および「読み→漢字」の逆引き確認に使う。
- カードのファイル形式(SVG / HTML / PNG 等)と1枚のレイアウトは、PDF組版に都合のよいものを選んでよい。

## 3. PDF仕様

- **B4縦(257×364mm)**に印刷する想定。家庭用プリンタで端が切れない程度の余白を取る。
- **少し大きめ**のコマで、1ページ 2〜3字を目安とする。画数の多い字はコマを複数行に折り返す。
- ヘッダに日付と自由記入の出典欄(例:「○○塾 漢字ドリル p.12」)があるとよい。
- モノクロ印刷でも「いま書く画」が判別できるよう、色だけでなく太さ・濃度でも差をつける。

## 4. 写真→PDF のワークフロー

1. 入力: ドリル問題の写真(1〜複数枚)。
2. Claude Code が問題文を読み取り、解答となる漢字を列挙する。読み取りに自信がない問題は、勝手に確定せず候補を親に確認する。
3. 特定した漢字のカードを集めて1つのPDFに組む。重複は1枚にまとめる。
4. 答えに小3配当外の漢字が含まれる場合: その旨を伝えたうえで、可能なら KanjiVG から即席でカードを生成して含める(読み仮名は無くても可)。
5. この一連の流れが「写真を渡す→(必要なら確認に答える)→PDFが出る」で完結すること。生成コマンドは1つにまとめ、使い方を README に書く。

## 5. データソースとライセンス

- **筆順**: [KanjiVG](https://github.com/KanjiVG/kanjivg)(CC BY-SA)。各字のSVGに筆順どおりの path と番号位置が入っている。パース方法は `gen_sample.py` 参照。
- **読み・画数**: [KANJIDIC2](https://www.edrdg.org/wiki/index.php/KANJIDIC_Project)(EDRDG, CC BY-SA)等。別ソースを使ってもよいが、出典が明確でライセンス上再配布可能なこと。
- **学年配当**: 文部科学省 学年別漢字配当表(現行課程)。1年80字・2年160字・3年200字。リポジトリ内に配当リストを持つこと。
- README に上記の attribution とライセンス表記を必ず入れる。

## 6. リポジトリ要件

- カード(生成物)と生成スクリプトの両方を管理し、いつでも再生成できること。
- 全カードをブラウザで一覧できる索引ページ(学年別・読み順など)があると便利。
- 将来、小4以降(4年202字)への拡張が容易な構成にする。
- CLAUDE.md に日常運用(写真→PDF)の手順を書き、親が毎回長い指示をしなくて済むようにする。

## 7. Web閲覧(GitHub Pages / iPad)

- リポジトリを GitHub Pages で公開し、**iPad の Safari から閲覧できる静的サイト**を提供する。push で自動的に更新されること(GitHub Actions 等)。
- サイトの内容:
  - **索引ページ**: 学年別・読み順で全カードを一覧し、各カードページへ飛べる。
  - **カードページ**: 紙と同じ一画分解表示に加え、画面ならではの表示(タップ/ボタンで一画ずつ進む・自動アニメーション等)があるとよい。答え合わせや書き直しの際に子どもと一緒に見る用途。
  - **「今回の課題」ページ**: ドリル写真からPDFを生成する際、同じ漢字セットのHTMLページも生成してコミットし、日付付きでサイトから開けるようにする。印刷用PDFとiPad用ページが常に対で揃う。
- スマホ・タブレットの画面幅で崩れないレスポンシブなレイアウトにする。
- **公開範囲の注意**: 無料プランの Pages は URL を知っていれば誰でも閲覧できる。ページ上には塾名・子どもの名前などの個人情報を載せないこと(出典欄のメモはPDF側のみに入れるか、伏せた表記にする)。

## 8. 受け入れ基準

- 440字すべてのカードが存在し、コマ数=画数、番号の連番が一致している(機械チェックを入れる)。
- 代表例の目視確認: 「右」と「左」で1画目が異なる(右は左払いから、左は横画から)、「田」「王」など筆順を誤りやすい字が正しい。
- サンプルの問題写真1枚から B4 PDF 生成までが通しで動く。
- 読み仮名がカード上で音=カタカナ/訓=ひらがなに正しく分かれている。
- Pages のサイトが iPad(Safari)で閲覧でき、索引→カード→「今回の課題」ページが機能する。

## 9. 同梱の参考実装

- `gen_sample.py` — KanjiVG のパースと一画分解コマ(SVG)生成。動作確認済み。設計上の都合で書き直してよい。
- `kakijun_sample.html` — 承認済みの見た目の見本(右・左・水・空)。
